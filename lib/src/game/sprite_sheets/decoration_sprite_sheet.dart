import 'package:bonfire/bonfire.dart';

class DecorationSpriteSheet {
  static Future<Sprite> get chest => Sprite.load(
    '',
    // srcPosition: Vector2(x, y),
    // srcSize: Vector2(x, y)
  );

  static Future<Sprite> get chestOpen => Sprite.load(
    '',
    // srcPosition: Vector2(x, y),
    // srcSize: Vector2(x, y)
  );
}
